#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
char s[10];
int i;
gets(s);
l=strlen(s);
for(i=0;i<l;i++)
{
if(isalpha(s[i])!=0)
{
printf("Alpha");
}
else
{
printf("No");
}
}
getch();
}
